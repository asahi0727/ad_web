---
import type { Category } from '../lib/categories';
import { renderPictogram } from '../lib/pictograms';

interface Props {
  category: Category;
  size?: number;
  class?: string;
}
const { category, size = 24, class: className } = Astro.props;
---
<Fragment set:html={renderPictogram(category, { size, className })} />
